import React from 'react'; import {Outlet,NavLink,useNavigate} from 'react-router-dom';
export default function Layout(){const nav=useNavigate(); const u=JSON.parse(localStorage.getItem('usuario')||'{}'); const logout=()=>{localStorage.clear();nav('/login')}; return <div className="app-shell"><aside className="sidebar"><div className="brand"><span className="brand-mark">C</span><div><strong>CEIA La Pintana</strong><small>Gestión interna</small></div></div><nav>
{[['/','Inicio'],['/cursos','Cursos'],['/estudiantes','Estudiantes'],['/asistencia','Asistencia'],['/inventario','Inventario'],['/agenda','Agenda'],['/usuarios','Usuarios']].map(([to,label])=><NavLink key={to} end={to==='/'} to={to} className={({isActive})=>isActive?'active':''}>{label}</NavLink>)}
</nav><div className="userbox"><small>{u.rol==='ADMIN'?'Administrador':'Profesor'}</small><strong>{u.first_name||u.username||'Usuario'}</strong><button onClick={logout}>Cerrar sesión</button></div></aside><main><header className="topbar"><div><h1>Sistema Web de Gestión Interna</h1><p>CEIA La Pintana</p></div><span className="status-pill">● Sistema conectado</span></header><section className="content"><Outlet/></section></main></div>}
