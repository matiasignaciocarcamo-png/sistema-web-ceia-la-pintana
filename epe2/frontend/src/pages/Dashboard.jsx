import React,{useEffect,useState} from 'react';import api from '../services/api';import StatCard from '../components/StatCard';import Loading from '../components/Loading';import ErrorMessage from '../components/ErrorMessage';
export default function Dashboard(){const [d,setD]=useState(null),[error,setError]=useState('');useEffect(()=>{api.get('dashboard/').then(r=>setD(r.data)).catch(()=>setError('No fue posible cargar el panel.'))},[]);if(!d&&!error)return <Loading/>;return <><ErrorMessage error={error}/>{d&&<><div className="page-heading"><div><h2>Panel principal</h2><p>Resumen de información prioritaria del establecimiento.</p></div><span>Actualización en línea</span></div><div className="stats-grid"><StatCard title="Estudiantes activos" value={d.total_estudiantes} caption="Registros vigentes"/><StatCard title="Cursos activos" value={d.total_cursos} caption="Jornadas configuradas"/><StatCard title="Alertas de asistencia" value={d.alertas_asistencia.length} caption="Bajo 85%"/><StatCard title="Stock crítico" value={d.productos_stock_bajo} caption="Productos por revisar"/></div><div className="grid-2"><div className="panel"><h3>Alertas de asistencia</h3>{d.alertas_asistencia.length===0?<p>Sin alertas activas.</p>:<table className="table"><thead><tr><th>Estudiante</th><th>Curso</th><th>Asistencia</th></tr></thead><tbody>{d.alertas_asistencia.map(a=><tr key={a.id}><td>{a.nombre}</td><td>{a.curso}</td><td><span className="badge text-bg-danger">{a.porcentaje}%</span></td></tr>)}</tbody></table>}</div><div className="panel"><h3>Próximos eventos</h3>{d.eventos_proximos.map(e=><div className="event-row" key={e.id}><strong>{e.titulo}</strong><span>{e.fecha}</span><small>{e.categoria}</small></div>)}</div></div></>}</>}
